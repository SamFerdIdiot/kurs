# 🚀 Быстрый Старт - Реализация Упрощённой Архитектуры

## 📋 Текущий Статус

✅ **Выполнено:**
- Side-view геймплей с движением только на A/D
- Детальный план упрощённой архитектуры
- Спецификация UI компонентов
- Визуальные макеты всех экранов

⏳ **Следующий шаг:**
Полная реализация всех компонентов за один запрос

---

## 🎯 Что Будем Делать

### Этап 1: SimpleActionMenu (1.5 часа)
**Файлы:** `include/SimpleActionMenu.h`, `src/SimpleActionMenu.cpp`

**Функциональность:**
- Popup меню, появляется при нажатии E возле структуры
- Навигация стрелками или цифрами 1-5
- Enter для выбора, ESC для отмены
- Визуальная подсветка выбранного пункта
- Мгновенное выполнение простых действий

**Интеграция в NodeScene:**
- Добавить `SimpleActionMenu m_actionMenu;`
- Обработка ввода через меню если оно видимо
- Отрисовка меню поверх игры

---

### Этап 2: SimpleScreen Base Class (1 час)
**Файлы:** `include/SimpleScreen.h`, `src/SimpleScreen.cpp`

**Функциональность:**
- Базовый класс для всех простых экранов
- Помощники для кнопок (Button struct)
- Обработка мыши и клавиатуры
- Стандартное оформление (фон, заголовок, кнопки)

---

### Этап 3: Structure Screens (2 часа)
**Файлы:**
- `include/GasStationScreen.h`, `src/GasStationScreen.cpp`
- `include/StoreScreen.h`, `src/StoreScreen.cpp`
- `include/MotelScreen.h`, `src/MotelScreen.cpp`

**GasStationScreen:**
- Кнопка "Refuel" (заправка на полный бак)
- Кнопка "Repair" (ремонт)
- Список товаров с кнопками "Buy"
- Кнопка "Back"

**StoreScreen:**
- Список товаров с постраничной навигацией
- Цена, вес, описание для каждого товара
- Кнопки "Buy" для каждого товара
- Кнопки навигации: Previous/Next
- Кнопка "Back"

**MotelScreen:**
- Кнопка "Quick Rest" ($20, +50 energy)
- Кнопка "Full Rest" ($40, +100 energy, auto-save)
- Дополнительные опции (душ, прачечная)
- Кнопка "Back"

---

### Этап 4: Quest & Inventory (1.5 часа)
**Файлы:**
- `include/QuestMapScreen.h`, `src/QuestMapScreen.cpp`
- `include/SimpleInventoryScreen.h`, `src/SimpleInventoryScreen.cpp`

**QuestMapScreen:**
- Список активных квестов (с прогрессом)
- Список доступных квестов
- Кнопки Accept/View для каждого квеста
- Информация о следующей точке путешествия
- Кнопки "Continue Journey" / "Back"

**SimpleInventoryScreen:**
- Список предметов с постраничной навигацией
- Для каждого предмета: иконка, название, вес, количество
- Кнопки "Use" и "Drop"
- Отображение общего веса
- Кнопки навигации + "Back"

---

### Этап 5: SceneManager Update (30 минут)
**Файл:** `src/SceneManager.cpp`

**Добавить в createScene:**
```cpp
case SceneType::GAS_STATION_SCREEN:
    return std::make_unique<GasStationScreen>();
case SceneType::STORE_SCREEN:
    return std::make_unique<StoreScreen>();
case SceneType::MOTEL_SCREEN:
    return std::make_unique<MotelScreen>();
case SceneType::QUEST_MAP:
    return std::make_unique<QuestMapScreen>();
case SceneType::SIMPLE_INVENTORY:
    return std::make_unique<SimpleInventoryScreen>();
```

**Обновить SceneType enum:**
```cpp
enum class SceneType {
    // ... existing
    GAS_STATION_SCREEN,
    STORE_SCREEN,
    MOTEL_SCREEN,
    QUEST_MAP,
    SIMPLE_INVENTORY,
};
```

---

### Этап 6: Cleanup (30 минут)
**Удалить:**
- `include/LocationScene.h`
- `src/LocationScene.cpp`
- Упоминания LocationScene в SceneManager

**Обновить:**
- `CMakeLists.txt` (удалить LocationScene, добавить новые файлы)
- `README.md` (обновить описание UI)

---

## 📝 Порядок Реализации

### 1. SimpleActionMenu
```bash
# Создаём файлы
touch include/SimpleActionMenu.h
touch src/SimpleActionMenu.cpp

# Реализуем:
1. Структуру MenuAction
2. Класс SimpleActionMenu
3. Методы show/hide/handleInput/render
4. Интеграцию в NodeScene
```

### 2. SimpleScreen
```bash
# Создаём файлы
touch include/SimpleScreen.h
touch src/SimpleScreen.cpp

# Реализуем:
1. Button struct
2. Базовый класс SimpleScreen
3. Методы createButton/handleInput/render
```

### 3. Structure Screens (по одному)
```bash
# GasStationScreen
touch include/GasStationScreen.h
touch src/GasStationScreen.cpp
# Реализуем + тестируем

# StoreScreen
touch include/StoreScreen.h
touch src/StoreScreen.cpp
# Реализуем + тестируем

# MotelScreen
touch include/MotelScreen.h
touch src/MotelScreen.cpp
# Реализуем + тестируем
```

### 4. Quest & Inventory
```bash
touch include/QuestMapScreen.h
touch src/QuestMapScreen.cpp
touch include/SimpleInventoryScreen.h
touch src/SimpleInventoryScreen.cpp
# Реализуем оба
```

### 5. Integration
```bash
# Обновляем SceneManager
# Обновляем CMakeLists.txt
# Удаляем LocationScene
# Компиляция и тест
```

---

## ✅ Чек-лист Проверки После Реализации

### Функциональность:
- [ ] SimpleActionMenu появляется при E возле структур
- [ ] Можно выбрать действие стрелками или цифрами
- [ ] Быстрые действия работают мгновенно
- [ ] "More Options" открывает соответствующий экран
- [ ] Все 3 structure screens работают
- [ ] QuestMapScreen отображает квесты
- [ ] SimpleInventoryScreen работает с предметами
- [ ] Можно вернуться из любого экрана в NodeScene
- [ ] Переходы между сценами плавные

### UI/UX:
- [ ] Все элементы видны и читаемы
- [ ] Подсветка работает корректно
- [ ] Кнопки реагируют на hover
- [ ] Цвета соответствуют схеме
- [ ] Шрифты загружены и отображаются
- [ ] Нет визуальных артефактов

### Код:
- [ ] Компиляция без ошибок
- [ ] Компиляция без предупреждений
- [ ] Нет утечек памяти
- [ ] LocationScene удалён
- [ ] CMakeLists.txt обновлён
- [ ] Все файлы добавлены в git

---

## 🎨 Визуальная Референция

### Цвета (copy-paste в код):
```cpp
const sf::Color BG_DARK(40, 40, 50);
const sf::Color BG_LIGHT(60, 60, 70);
const sf::Color ACCENT_BLUE(70, 130, 180);
const sf::Color ACCENT_GREEN(60, 180, 75);
const sf::Color BUTTON_NORMAL(80, 80, 90);
const sf::Color BUTTON_HOVER(100, 100, 110);
const sf::Color TEXT_WHITE(255, 255, 255);
const sf::Color BORDER(150, 150, 160);
```

### Размеры:
```cpp
const float PADDING = 10.0f;
const float BUTTON_HEIGHT = 40.0f;
const float BUTTON_WIDTH = 200.0f;
const unsigned int FONT_SIZE = 18;
```

---

## 🚀 Команды для Быстрого Старта

```bash
# 1. Создать все header файлы
cd /home/runner/work/kkurs/kkurs/include
touch SimpleActionMenu.h SimpleScreen.h
touch GasStationScreen.h StoreScreen.h MotelScreen.h
touch QuestMapScreen.h SimpleInventoryScreen.h

# 2. Создать все source файлы
cd /home/runner/work/kkurs/kkurs/src
touch SimpleActionMenu.cpp SimpleScreen.cpp
touch GasStationScreen.cpp StoreScreen.cpp MotelScreen.cpp
touch QuestMapScreen.cpp SimpleInventoryScreen.cpp

# 3. После реализации - обновить CMake
cd /home/runner/work/kkurs/kkurs/build
cmake ..
make -j4

# 4. Запустить игру
./kkurs
```

---

## 📚 Документация

**Детальные планы:**
- `SIMPLIFIED_ARCHITECTURE_PLAN.md` - Общая архитектура
- `UI_COMPONENTS_SPEC.md` - Детали реализации компонентов

**Визуальные макеты:**
- Все экраны описаны с ASCII-артом в планах

---

## ⏱️ Оценка Времени

| Этап | Время | Статус |
|------|-------|--------|
| SimpleActionMenu | 1.5h | ⏳ Ready |
| SimpleScreen | 1h | ⏳ Ready |
| Structure Screens | 2h | ⏳ Ready |
| Quest & Inventory | 1.5h | ⏳ Ready |
| SceneManager Update | 0.5h | ⏳ Ready |
| Cleanup | 0.5h | ⏳ Ready |
| **TOTAL** | **7h** | **Ready to Start** |

---

## 🎯 Критерий Успеха

**Игра готова к релизу когда:**
1. ✅ Можно играть в NodeScene (A/D движение)
2. ✅ Можно взаимодействовать со структурами (E + меню)
3. ✅ Быстрые действия работают мгновенно
4. ✅ Можно открыть детальные экраны структур
5. ✅ Можно открыть инвентарь (I)
6. ✅ Можно открыть карту квестов (M)
7. ✅ Всё работает без багов
8. ✅ UI понятный и быстрый

---

## 💪 Готовы к Реализации!

**В следующем запросе:**
"Начинаем полную реализацию! Делаем поэтапно все 7 компонентов. Готов работать долго и тщательно."

**Ожидаемый результат:**
Полностью рабочая игра с упрощённым UI, готовая к релизу ✨

---

*Создано: 2025-11-17*
*Готов к старту: ✅*
