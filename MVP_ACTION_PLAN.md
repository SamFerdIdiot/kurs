# MVP ACTION PLAN - План действий для создания минимальной версии

## 🎯 ЦЕЛЬ
Упростить игру до простого цикла: Меню → Карта → Поездка → Город → ... → Финал

---

## 📋 ТЕКУЩЕЕ СОСТОЯНИЕ

### Что работает сейчас:
- ✅ MainMenuScene - главное меню
- ✅ MapScene - карта с городами
- ✅ TravelCinematicScene - анимация поездки
- ✅ LocationScene - сцена города
- ✅ PlayerState - состояние игрока (с ресурсами)
- ✅ EventManager - 47 событий
- ✅ CinematicEventScene - показ событий
- ✅ HUDPanel - индикаторы ресурсов

### Что нужно убрать:
- ❌ Топливо
- ❌ Энергия
- ❌ Деньги
- ❌ Состояние машины
- ❌ События во время поездки
- ❌ Диалоги с NPC
- ❌ Компаньоны
- ❌ Сервисы в городах
- ❌ HUD Panel (индикаторы)

### Что нужно создать:
- 📝 EndGameScene - финальная сцена

---

## 🔨 ПЛАН РАБОТЫ

### ЭТАП 1: Создать EndGameScene (30 мин)

**Файлы:** `include/EndGameScene.h`, `src/EndGameScene.cpp`

**Функционал:**
- Поздравление
- Статистика (время, города)
- Кнопка "Вернуться в меню"

**Код:**
```cpp
class EndGameScene : public Scene {
private:
    sf::Font m_font;
    sf::Text m_titleText;
    sf::Text m_statsText;
    sf::RectangleShape m_menuButton;
    sf::Text m_menuButtonText;
    float m_totalTime;
    int m_citiesVisited;

public:
    EndGameScene(float totalTime, int citiesVisited);
    void handleInput(const sf::Event& event) override;
    void update(float deltaTime) override;
    void render(sf::RenderWindow& window) override;
    SceneType getNextScene() const override;
    bool isFinished() const override;
};
```

---

### ЭТАП 2: Упростить PlayerState (20 мин)

**Файл:** `src/PlayerState.cpp`, `include/PlayerState.h`

**Действия:**
1. Закомментировать fuel, energy, money
2. Оставить только:
   - currentCityIndex
   - totalPlayTime
   - isGameFinished

**Было:**
```cpp
class PlayerState {
private:
    float m_fuel;
    float m_energy;
    float m_money;
    GridInventorySystem m_inventory;
    std::vector<NPC> m_companions;
    // ...
};
```

**Стало:**
```cpp
class PlayerState {
private:
    int m_currentCityIndex = 0;
    float m_totalPlayTime = 0.0f;
    bool m_isGameFinished = false;

public:
    // Только базовые методы
    int getCurrentCityIndex() const { return m_currentCityIndex; }
    void setCurrentCityIndex(int index) { m_currentCityIndex = index; }

    float getTotalPlayTime() const { return m_totalPlayTime; }
    void updatePlayTime(float delta) { m_totalPlayTime += delta; }

    bool isGameFinished() const { return m_isGameFinished; }
    void finishGame() { m_isGameFinished = true; }
};
```

---

### ЭТАП 3: Упростить TravelCinematicScene (30 мин)

**Файл:** `src/TravelCinematicScene.cpp`

**Действия:**
1. Убрать проверку событий
2. Убрать диалоги
3. Убрать расход топлива/энергии
4. Убрать HUD
5. Оставить только:
   - Параллакс фон
   - Анимация машины
   - Прогресс-бар
   - Автозавершение

**Закомментировать:**
```cpp
// ❌ Убрать это:
// checkForRandomEvent();
// updateDialogues(deltaTime);
// m_playerState->consumeFuel(fuelRate * deltaTime);
// m_playerState->consumeEnergy(energyRate * deltaTime);
// m_hudPanel.render(window);
```

**Оставить:**
```cpp
// ✅ Оставить это:
updateParallax(deltaTime);
updateCarAnimation(deltaTime);
m_travelProgress += deltaTime / m_travelDuration;
if (m_travelProgress >= 1.0f) {
    m_isFinished = true;
}
```

---

### ЭТАП 4: Упростить LocationScene (20 мин)

**Файл:** `src/LocationScene.cpp`

**Действия:**
1. Убрать кнопки сервисов (заправка, мотель, магазин)
2. Оставить только:
   - Название города
   - Описание
   - Кнопка "Продолжить путешествие"

**Закомментировать:**
```cpp
// ❌ Убрать кнопки:
// m_refuelButton
// m_motelButton
// m_shopButton
// m_repairButton
```

**Оставить:**
```cpp
// ✅ Оставить:
m_cityNameText
m_descriptionText
m_continueButton
```

---

### ЭТАП 5: Упростить MapScene (30 мин)

**Файл:** `src/MapScene.cpp`

**Действия:**
1. Убрать HUD
2. Упростить логику выбора города
3. Оставить только:
   - Список 5 городов
   - Текущая позиция (выделена)
   - Следующий город (можно выбрать)
   - Кнопка "Начать путешествие"

**Закомментировать:**
```cpp
// ❌ Убрать:
// m_hudPanel.render(window);
// Проверки топлива/энергии
```

**Добавить:**
```cpp
// ✅ Добавить проверку финиша:
if (m_playerState->getCurrentCityIndex() >= 4) {
    // Переход в EndGameScene
    m_nextScene = SceneType::END_GAME;
    m_isFinished = true;
}
```

---

### ЭТАП 6: Обновить SceneManager (15 мин)

**Файл:** `src/SceneManager.cpp`, `include/Scene.h`

**Действия:**
1. Добавить SceneType::END_GAME в enum
2. Добавить создание EndGameScene

**Добавить в Scene.h:**
```cpp
enum class SceneType {
    NONE,
    MAIN_MENU,
    CHARACTER_CREATION,
    MAP,
    LOCATION,
    TRAVEL_CINEMATIC,
    CINEMATIC_EVENT,
    // ... другие
    END_GAME  // ✅ Добавить это
};
```

**Добавить в SceneManager.cpp:**
```cpp
case SceneType::END_GAME: {
    float totalTime = m_playerState.getTotalPlayTime();
    int cities = m_playerState.getCurrentCityIndex() + 1;
    m_currentScene = std::make_unique<EndGameScene>(totalTime, cities);
    break;
}
```

---

### ЭТАП 7: Убрать HUD из всех сцен (15 мин)

**Файлы:** Все сцены с HUD

**Действия:**
Закомментировать в каждой сцене:
```cpp
// ❌ Убрать из update():
// m_hudPanel.setFuel(...);
// m_hudPanel.setEnergy(...);
// m_hudPanel.setMoney(...);
// m_hudPanel.update(deltaTime);

// ❌ Убрать из render():
// m_hudPanel.render(window);
```

**Сцены для изменения:**
- MapScene
- LocationScene
- TravelCinematicScene
- CinematicEventScene (уже не будет вызываться)

---

### ЭТАП 8: Создать простой список городов (15 мин)

**Файл:** `src/GameStateManager.cpp` или новый `Cities.h`

**Создать данные городов:**
```cpp
// Cities.h
#pragma once
#include <vector>
#include <string>
#include <SFML/System/Vector2.hpp>

struct CityData {
    std::string name;
    std::string description;
    sf::Vector2f mapPosition;
};

const std::vector<CityData> CITIES = {
    {
        "Москва",
        "Столица России. Начало вашего путешествия через всю страну.",
        {200.0f, 400.0f}
    },
    {
        "Нижний Новгород",
        "Волжский город на слиянии двух великих рек.",
        {400.0f, 350.0f}
    },
    {
        "Казань",
        "Столица Татарстана. Город двух культур.",
        {600.0f, 300.0f}
    },
    {
        "Екатеринбург",
        "Уральский город на границе Европы и Азии.",
        {900.0f, 350.0f}
    },
    {
        "Владивосток",
        "Тихоокеанский порт. Финиш вашего путешествия!",
        {1200.0f, 400.0f}
    }
};
```

---

### ЭТАП 9: Обновить CMakeLists.txt (5 мин)

**Файл:** `CMakeLists.txt`

**Действия:**
Добавить EndGameScene в список исходников:
```cmake
add_executable(kkurs
    # ... другие файлы
    src/EndGameScene.cpp
)
```

---

### ЭТАП 10: Тестирование MVP (30 мин)

**Тесты:**
1. ✅ Запуск игры
2. ✅ Главное меню → Новая игра
3. ✅ Карта показывает 5 городов
4. ✅ Выбор города 2 → Поездка
5. ✅ Анимация поездки работает
6. ✅ Прибытие в город 2
7. ✅ "Продолжить" → Карта
8. ✅ Повторить для городов 3, 4, 5
9. ✅ После города 5 → EndGameScene
10. ✅ "Вернуться в меню" → Главное меню

---

## 🕐 ОЦЕНКА ВРЕМЕНИ

| Этап | Время | Сложность |
|------|-------|-----------|
| 1. EndGameScene | 30 мин | Легко |
| 2. PlayerState | 20 мин | Легко |
| 3. TravelCinematicScene | 30 мин | Средне |
| 4. LocationScene | 20 мин | Легко |
| 5. MapScene | 30 мин | Средне |
| 6. SceneManager | 15 мин | Легко |
| 7. Убрать HUD | 15 мин | Легко |
| 8. Города | 15 мин | Легко |
| 9. CMakeLists | 5 мин | Легко |
| 10. Тестирование | 30 мин | - |
| **ИТОГО** | **3.5 часа** | |

---

## 📝 ЧЕКЛИСТ ПЕРЕД НАЧАЛОМ

Перед началом работы проверь:
- [ ] Сделан git commit текущего состояния
- [ ] Создана новая ветка `mvp-simplification`
- [ ] Прочитан MVP_CORE_DESIGN.md
- [ ] Прочитан FEATURES_CATALOG.md
- [ ] Понятен план действий

---

## 🚀 НАЧАЛО РАБОТЫ

Когда готов начать, скажи:
```
Начинаем упрощение до MVP. Начнем с этапа 1: создание EndGameScene.
```

И я начну выполнять план пошагово!

---

## 📊 ПОСЛЕ ЗАВЕРШЕНИЯ MVP

После создания MVP у нас будет:

**✅ Работает:**
- Главное меню
- Карта с 5 городами
- Выбор следующего города
- Анимация поездки (10 сек)
- Прибытие в город
- Простое описание города
- Кнопка "Продолжить"
- Финальный экран после 5 города
- Возврат в меню

**❌ Не работает (временно отключено):**
- Ресурсы (топливо, энергия, деньги)
- События в дороге
- Диалоги
- Компаньоны
- Сервисы в городах
- HUD индикаторы
- Прогрессия (опыт, способности)
- Инвентарь
- Квесты

**🎯 Результат:**
Чистый каркас игры с основным геймплей лупом, готовый для постепенного добавления фич.

---

## 🔄 СЛЕДУЮЩИЕ ШАГИ ПОСЛЕ MVP

1. Протестировать MVP
2. Зафиксировать в git как baseline
3. Создать FEATURE_TOGGLE систему
4. Добавлять фичи по одной из FEATURES_CATALOG.md
5. Тестировать после каждой фичи
6. Балансировать игру

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

1. **НЕ УДАЛЯТЬ КОД** - только комментировать
2. **СОХРАНЯТЬ СТРУКТУРУ** - классы и файлы остаются
3. **ДОКУМЕНТИРОВАТЬ** - помечать что отключено
4. **ТЕСТИРОВАТЬ** - после каждого этапа
5. **КОММИТИТЬ** - после каждого рабочего состояния

Используй комментарии:
```cpp
// [MVP] Feature disabled temporarily
// [MVP] Original code:
// код...
```

Это позволит легко вернуть фичи позже!
