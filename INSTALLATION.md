# 🚀 ИНСТРУКЦИЯ ПО УСТАНОВКЕ И ЗАПУСКУ

**Версия:** MVP 1.0
**Дата:** 2025-12-21

---

## 📋 Системные требования

### Минимальные требования
- **ОС:** macOS 10.15+, Linux (Ubuntu 20.04+), Windows 10+
- **RAM:** 2 GB
- **Процессор:** Dual-core 2.0 GHz
- **Свободное место:** 50 MB

### Необходимые инструменты
- **C++ компилятор:** поддержка C++17
  - macOS: Apple Clang 12.0+
  - Linux: GCC 9.0+ или Clang 10.0+
  - Windows: MSVC 2019+ или MinGW-w64
- **CMake:** версия 3.10 или выше
- **SFML:** версия 2.5.1 или выше

---

## 🍎 Установка на macOS

### Шаг 1: Установка Homebrew (если не установлен)

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### Шаг 2: Установка зависимостей

```bash
# Установить CMake и SFML
brew install cmake sfml

# Проверить версии
cmake --version  # Должно быть >= 3.10
```

### Шаг 3: Клонирование репозитория

```bash
# Клонировать проект
git clone <repository-url>
cd kurs
```

### Шаг 4: Сборка проекта

```bash
# Создать директорию для сборки
mkdir -p build
cd build

# Конфигурация CMake
cmake .. -DSFML_DIR=/opt/homebrew/opt/sfml@2/lib/cmake/SFML

# Сборка (используем 4 ядра для ускорения)
make -j4
```

### Шаг 5: Запуск игры

```bash
# Запустить игру
./kkurs
```

### Опционально: Запуск тестов

```bash
# Из директории build/
./tests/unit_tests

# Ожидаемый результат:
# [  PASSED  ] 78 tests.
```

---

## 🐧 Установка на Linux (Ubuntu/Debian)

### Шаг 1: Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### Шаг 2: Установка зависимостей

```bash
# Установить компилятор, CMake и SFML
sudo apt install -y build-essential cmake git
sudo apt install -y libsfml-dev

# Проверить версии
cmake --version  # Должно быть >= 3.10
g++ --version    # Должно быть >= 9.0
```

### Шаг 3: Клонирование и сборка

```bash
# Клонировать проект
git clone <repository-url>
cd kurs

# Создать директорию для сборки
mkdir -p build
cd build

# Конфигурация и сборка
cmake ..
make -j$(nproc)

# Запуск
./kkurs
```

---

## 🪟 Установка на Windows

### Вариант 1: Visual Studio

#### Шаг 1: Установка Visual Studio 2019/2022

Загрузить и установить [Visual Studio](https://visualstudio.microsoft.com/)

Выбрать компоненты:
- Desktop development with C++
- CMake tools for Windows

#### Шаг 2: Установка SFML

1. Скачать SFML 2.5.1+ для Visual C++ с [официального сайта](https://www.sfml-dev.org/download.php)
2. Распаковать в `C:\SFML`

#### Шаг 3: Клонирование проекта

```cmd
git clone <repository-url>
cd kurs
```

#### Шаг 4: Сборка через Visual Studio

1. Открыть проект: File → Open → CMake → выбрать CMakeLists.txt
2. Указать путь к SFML в CMake settings
3. Build → Build All
4. Debug → Start (или Ctrl+F5)

### Вариант 2: MinGW-w64 + CMake

```cmd
# Установить MinGW-w64, CMake, Git
# Скачать SFML для MinGW

# Сборка
mkdir build
cd build
cmake .. -G "MinGW Makefiles" -DSFML_DIR=C:/SFML/lib/cmake/SFML
mingw32-make

# Запуск
kkurs.exe
```

---

## 🔧 Возможные проблемы и решения

### Проблема: CMake не находит SFML

**Решение (macOS):**
```bash
# Найти путь к SFML
brew info sfml

# Использовать полный путь при конфигурации
cmake .. -DSFML_DIR=/opt/homebrew/opt/sfml@2/lib/cmake/SFML
```

**Решение (Linux):**
```bash
# Переустановить SFML
sudo apt remove libsfml-dev
sudo apt install libsfml-dev

# Или указать путь вручную
cmake .. -DSFML_DIR=/usr/lib/x86_64-linux-gnu/cmake/SFML
```

### Проблема: Ошибка компиляции "C++17 required"

**Решение:**
```bash
# Обновить компилятор
# macOS:
brew upgrade gcc

# Linux:
sudo apt install g++-9
export CXX=g++-9
```

### Проблема: Шрифт не загружается

**Решение:**
```bash
# Проверить наличие шрифта
ls assets/fonts/font.ttf

# Если нет, скопировать из корня проекта
cp -r assets build/
```

### Проблема: "SFML window не открывается"

**Решение (macOS):**
Требуется разрешение на отображение окон. Проверить System Preferences → Security & Privacy.

**Решение (Linux):**
```bash
# Установить дополнительные библиотеки
sudo apt install libxrandr-dev libxcursor-dev libxi-dev
```

### Проблема: Тесты не запускаются

**Решение:**
```bash
# Пересобрать с тестами
cd build
cmake .. -DBUILD_TESTS=ON
make unit_tests

# Запустить
./tests/unit_tests
```

---

## 📦 Release сборка (оптимизированная)

Для финальной версии с оптимизацией:

```bash
# Создать отдельную директорию
mkdir build_release
cd build_release

# Конфигурация в Release режиме
cmake .. -DCMAKE_BUILD_TYPE=Release -DSFML_DIR=/opt/homebrew/opt/sfml@2/lib/cmake/SFML

# Сборка
make -j4

# Запуск
./kkurs
```

Release версия:
- ✅ Оптимизирована по скорости (-O3)
- ✅ Меньший размер (818 KB vs 3+ MB)
- ✅ Без отладочных символов

---

## 🎮 Управление игрой

После успешного запуска:

- **1, 2, 3, 4** - выбор варианта ответа
- **Enter** - подтвердить выбор
- **Space** - ускорить печать текста
- **ESC** - выход в главное меню

---

## 📊 Проверка установки

Для проверки корректности установки:

```bash
# 1. Запустить игру
./kkurs
# Должно открыться окно с главным меню

# 2. Запустить тесты
./tests/unit_tests
# Результат: [  PASSED  ] 78 tests.

# 3. Проверить сохранения
ls saves/
# Должны появиться файлы savegame.sav после игры
```

---

## 🆘 Получение помощи

Если возникли проблемы:

1. Проверьте раздел "Возможные проблемы" выше
2. Убедитесь, что все зависимости установлены корректно
3. Проверьте версии: `cmake --version`, `g++ --version`
4. Проверьте логи сборки на наличие ошибок

---

## 📚 Дополнительные ресурсы

- **Документация проекта:** [README.md](README.md)
- **Архитектура системы:** [docs/SYSTEM_ARCHITECTURE.md](docs/SYSTEM_ARCHITECTURE.md)
- **Руководство разработчика:** [docs/HOW_TO_ADD_CONTENT.md](docs/HOW_TO_ADD_CONTENT.md)
- **SFML документация:** https://www.sfml-dev.org/documentation/
- **CMake документация:** https://cmake.org/documentation/

---

**Удачной игры! 🎮**
