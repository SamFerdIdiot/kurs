# Формат данных карты мира / World Map Data Format

**Дата:** 2025-11-15  
**Статус:** Утверждено владельцем  
**Формат:** JSON

---

## 🗺️ Обзор

Карта мира создаётся **вручную** (без процедурной генерации) и хранится в формате **JSON**.

---

## 📄 Структура JSON файла

### Полный пример

```json
{
  "map_name": "Road Trip Russia 90s",
  "version": "1.0",
  "width": 1920,
  "height": 1080,
  "description": "Карта путешествия по России 90-х годов",
  
  "locations": [
    {
      "id": "loc_001",
      "name": "Москва",
      "name_en": "Moscow",
      "type": "capital",
      "size": "large",
      "x": 300,
      "y": 200,
      "structures": [
        {
          "type": "shop",
          "name": "Продуктовый магазин"
        },
        {
          "type": "workshop",
          "name": "Автосервис 'Мастер'"
        },
        {
          "type": "motel",
          "name": "Гостиница 'Интурист'"
        },
        {
          "type": "work_agency",
          "name": "Биржа труда"
        },
        {
          "type": "quest_board",
          "name": "Доска объявлений"
        }
      ],
      "description": "Столица и начальная точка путешествия"
    },
    {
      "id": "loc_002",
      "name": "Владимир",
      "name_en": "Vladimir",
      "type": "town",
      "size": "medium",
      "x": 500,
      "y": 250,
      "structures": [
        {
          "type": "shop",
          "name": "Магазин 'У дороги'"
        },
        {
          "type": "workshop",
          "name": "Гараж"
        },
        {
          "type": "motel",
          "name": "Придорожный мотель"
        }
      ],
      "description": "Древний город на Золотом кольце"
    },
    {
      "id": "loc_003",
      "name": "Нижний Новгород",
      "name_en": "Nizhny Novgorod",
      "type": "city",
      "size": "large",
      "x": 800,
      "y": 300,
      "structures": [
        {
          "type": "shop",
          "name": "Рынок"
        },
        {
          "type": "workshop",
          "name": "Автосервис"
        },
        {
          "type": "motel",
          "name": "Гостиница 'Волга'"
        },
        {
          "type": "work_agency",
          "name": "Служба занятости"
        },
        {
          "type": "quest_board",
          "name": "Доска объявлений"
        }
      ],
      "description": "Крупный промышленный город"
    }
  ],
  
  "roads": [
    {
      "id": "road_001",
      "from": "loc_001",
      "to": "loc_002",
      "type": "highway",
      "distance": 180,
      "name": "M7 'Волга'",
      "description": "Федеральная трасса в хорошем состоянии"
    },
    {
      "id": "road_002",
      "from": "loc_002",
      "to": "loc_003",
      "type": "road",
      "distance": 250,
      "name": "Дорога через Гороховец",
      "description": "Обычная дорога регионального значения"
    }
  ],
  
  "landmarks": [
    {
      "id": "landmark_001",
      "name": "Золотые ворота",
      "name_en": "Golden Gates",
      "type": "monument",
      "location_id": "loc_002",
      "description": "Знаменитые Золотые ворота XII века"
    },
    {
      "id": "landmark_002",
      "name": "Река Волга",
      "name_en": "Volga River",
      "type": "river",
      "points": [
        [700, 250],
        [750, 280],
        [800, 300],
        [850, 320]
      ],
      "description": "Великая русская река"
    }
  ]
}
```

---

## 📋 Спецификация элементов

### 1. Корневые поля

| Поле | Тип | Описание |
|------|-----|----------|
| `map_name` | string | Название карты |
| `version` | string | Версия формата (для совместимости) |
| `width` | integer | Ширина карты в пикселях |
| `height` | integer | Высота карты в пикселях |
| `description` | string | Описание карты |

---

### 2. Locations (Локации)

**Массив `locations`** - основные точки на карте, где игрок может остановиться.

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `id` | string | ✅ | Уникальный идентификатор |
| `name` | string | ✅ | Название (русское) |
| `name_en` | string | ⚪ | Название (английское) |
| `type` | string | ✅ | Тип локации (см. ниже) |
| `size` | string | ✅ | Размер (small/medium/large) |
| `x` | integer | ✅ | Координата X на карте |
| `y` | integer | ✅ | Координата Y на карте |
| `structures` | array | ✅ | Доступные структуры |
| `description` | string | ⚪ | Описание локации |

**Типы локаций (`type`):**
- `capital` - Столица (Москва)
- `city` - Крупный город
- `town` - Город
- `village` - Деревня
- `rest_stop` - Придорожная остановка
- `gas_station` - Автозаправочная станция

**Размеры (`size`):**
- `small` - Маленькая (1-2 структуры)
- `medium` - Средняя (3-4 структуры)
- `large` - Большая (5+ структур)

---

### 3. Structures (Структуры в локации)

**Массив `structures`** внутри каждой локации.

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `type` | string | ✅ | Тип структуры (см. ниже) |
| `name` | string | ✅ | Название конкретной структуры |

**Типы структур (`type`):**
- `shop` - Магазин (покупка еды, воды, инструментов)
- `workshop` - Мастерская (ремонт, заправка)
- `motel` - Мотель (отдых, сон, сохранение)
- `work_agency` - Агентство работы (подработка)
- `quest_board` - Доска объявлений (квесты)

---

### 4. Roads (Дороги)

**Массив `roads`** - связи между локациями.

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `id` | string | ✅ | Уникальный идентификатор |
| `from` | string | ✅ | ID начальной локации |
| `to` | string | ✅ | ID конечной локации |
| `type` | string | ✅ | Тип дороги (см. ниже) |
| `distance` | integer | ✅ | Расстояние в км |
| `name` | string | ⚪ | Название дороги |
| `description` | string | ⚪ | Описание |

**Типы дорог (`type`):**
- `highway` - Шоссе (федеральная трасса, низкий расход топлива)
- `road` - Дорога (обычная дорога, средний расход)
- `path` - Тропа (плохая дорога, высокий расход)

**Влияние типа на геймплей:**
- `highway`: -20% расход топлива, +20% скорость
- `road`: базовый расход и скорость
- `path`: +40% расход топлива, -20% скорость

---

### 5. Landmarks (Достопримечательности)

**Массив `landmarks`** - визуальные элементы карты (не обязательно для геймплея).

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `id` | string | ✅ | Уникальный идентификатор |
| `name` | string | ✅ | Название |
| `name_en` | string | ⚪ | Английское название |
| `type` | string | ✅ | Тип (см. ниже) |
| `location_id` | string | ⚪ | Привязка к локации (опционально) |
| `points` | array | ⚪ | Массив точек [x,y] для линий/полигонов |
| `description` | string | ⚪ | Описание |

**Типы landmarks (`type`):**
- `monument` - Памятник (привязан к локации)
- `river` - Река (линия из точек)
- `mountain` - Горы (полигон)
- `forest` - Лес (полигон)
- `lake` - Озеро (полигон)

---

## 🎨 Визуализация карты

### Рендеринг элементов

```cpp
class WorldMapRenderer {
public:
    void loadMap(const std::string& jsonPath) {
        // Парсинг JSON
        json mapData = parseJSON(jsonPath);
        
        // Загрузка локаций
        for (auto& loc : mapData["locations"]) {
            m_locations.push_back(Location(loc));
        }
        
        // Загрузка дорог
        for (auto& road : mapData["roads"]) {
            m_roads.push_back(Road(road));
        }
        
        // Загрузка landmarks
        for (auto& lm : mapData["landmarks"]) {
            m_landmarks.push_back(Landmark(lm));
        }
    }
    
    void render(sf::RenderWindow& window) {
        // 1. Рендер фона (текстура карты)
        renderBackground(window);
        
        // 2. Рендер landmarks (реки, горы)
        for (auto& lm : m_landmarks) {
            lm.render(window);
        }
        
        // 3. Рендер дорог (линии между локациями)
        for (auto& road : m_roads) {
            road.render(window);
        }
        
        // 4. Рендер локаций (иконки городов)
        for (auto& loc : m_locations) {
            loc.render(window);
        }
        
        // 5. Рендер позиции игрока
        renderPlayerPosition(window);
    }
};
```

---

## 🛠️ Инструменты создания карты

### Вариант 1: Ручное редактирование JSON

**Процесс:**
1. Открыть `world_map.json` в текстовом редакторе
2. Добавить локации с координатами
3. Добавить дороги между локациями
4. Сохранить и протестировать в игре

**Плюсы:** Просто, не требует доп. инструментов  
**Минусы:** Сложно визуализировать координаты

### Вариант 2: Простой визуальный редактор (Python)

Создать простой редактор на Python + Tkinter:

```python
import tkinter as tk
import json

class MapEditor:
    def __init__(self):
        self.root = tk.Tk()
        self.canvas = tk.Canvas(self.root, width=1920, height=1080)
        self.canvas.pack()
        
        self.locations = []
        self.roads = []
        
        # Клик для добавления локации
        self.canvas.bind("<Button-1>", self.add_location)
        
    def add_location(self, event):
        x, y = event.x, event.y
        name = input("Название локации: ")
        
        location = {
            "id": f"loc_{len(self.locations)+1:03d}",
            "name": name,
            "x": x,
            "y": y
        }
        
        self.locations.append(location)
        self.canvas.create_oval(x-5, y-5, x+5, y+5, fill="red")
        
    def save_map(self):
        data = {
            "locations": self.locations,
            "roads": self.roads
        }
        
        with open("world_map.json", "w") as f:
            json.dump(data, f, indent=2)
```

**Плюсы:** Визуальное размещение локаций  
**Минусы:** Требует Python

### Вариант 3: Использовать Tiled Map Editor

**Tiled** - популярный редактор карт.

**Процесс:**
1. Создать карту в Tiled
2. Экспортировать в JSON
3. Конвертировать формат в наш формат (простой скрипт)

**Плюсы:** Мощный инструмент  
**Минусы:** Нужна конвертация формата

---

## 📊 Пример готовой карты

### Маршрут: Москва → Владивосток (7000 км)

```json
{
  "map_name": "Транссибирская дорога",
  "width": 3000,
  "height": 1200,
  
  "locations": [
    {"id": "loc_001", "name": "Москва", "type": "capital", "x": 100, "y": 600},
    {"id": "loc_002", "name": "Казань", "type": "city", "x": 400, "y": 550},
    {"id": "loc_003", "name": "Екатеринбург", "type": "city", "x": 700, "y": 500},
    {"id": "loc_004", "name": "Новосибирск", "type": "city", "x": 1200, "y": 550},
    {"id": "loc_005", "name": "Иркутск", "type": "city", "x": 1800, "y": 600},
    {"id": "loc_006", "name": "Улан-Удэ", "type": "town", "x": 2000, "y": 650},
    {"id": "loc_007", "name": "Чита", "type": "city", "x": 2200, "y": 600},
    {"id": "loc_008", "name": "Хабаровск", "type": "city", "x": 2600, "y": 550},
    {"id": "loc_009", "name": "Владивосток", "type": "city", "x": 2900, "y": 600}
  ],
  
  "roads": [
    {"from": "loc_001", "to": "loc_002", "type": "highway", "distance": 800},
    {"from": "loc_002", "to": "loc_003", "type": "highway", "distance": 1200},
    {"from": "loc_003", "to": "loc_004", "type": "highway", "distance": 1400},
    {"from": "loc_004", "to": "loc_005", "type": "highway", "distance": 1800},
    {"from": "loc_005", "to": "loc_006", "type": "road", "distance": 400},
    {"from": "loc_006", "to": "loc_007", "type": "road", "distance": 450},
    {"from": "loc_007", "to": "loc_008", "type": "highway", "distance": 1500},
    {"from": "loc_008", "to": "loc_009", "type": "highway", "distance": 800}
  ]
}
```

**Общая длина:** ~8000 км  
**Время в пути:** ~80-100 часов игрового времени  
**Локаций:** 9  
**Дорог:** 8

---

## 🔧 Загрузка карты в игре

### Класс WorldMap

```cpp
class WorldMap {
public:
    bool loadFromFile(const std::string& jsonPath) {
        std::ifstream file(jsonPath);
        if (!file.is_open()) {
            std::cerr << "Failed to open map: " << jsonPath << std::endl;
            return false;
        }
        
        json mapData;
        file >> mapData;
        
        // Парсинг
        m_name = mapData["map_name"];
        m_width = mapData["width"];
        m_height = mapData["height"];
        
        // Локации
        for (auto& loc : mapData["locations"]) {
            Location location;
            location.id = loc["id"];
            location.name = loc["name"];
            location.type = parseLocationType(loc["type"]);
            location.x = loc["x"];
            location.y = loc["y"];
            
            // Структуры
            for (auto& str : loc["structures"]) {
                Structure s;
                s.type = parseStructureType(str["type"]);
                s.name = str["name"];
                location.structures.push_back(s);
            }
            
            m_locations[location.id] = location;
        }
        
        // Дороги
        for (auto& road : mapData["roads"]) {
            Road r;
            r.id = road["id"];
            r.from = road["from"];
            r.to = road["to"];
            r.type = parseRoadType(road["type"]);
            r.distance = road["distance"];
            
            m_roads.push_back(r);
        }
        
        return true;
    }
    
    Location* getLocation(const std::string& id) {
        auto it = m_locations.find(id);
        return (it != m_locations.end()) ? &it->second : nullptr;
    }
    
    std::vector<Road> getRoadsFrom(const std::string& locationId) {
        std::vector<Road> result;
        for (auto& road : m_roads) {
            if (road.from == locationId) {
                result.push_back(road);
            }
        }
        return result;
    }
    
private:
    std::string m_name;
    int m_width, m_height;
    std::map<std::string, Location> m_locations;
    std::vector<Road> m_roads;
    std::vector<Landmark> m_landmarks;
};
```

---

## ✅ Критерии приёмки

Формат готов, когда:

- [ ] JSON валидный и парсится без ошибок
- [ ] Все локации имеют уникальные ID
- [ ] Все дороги ссылаются на существующие локации
- [ ] Координаты в пределах размеров карты
- [ ] Каждая локация имеет хотя бы одну структуру
- [ ] Дороги соединяют локации в связный граф
- [ ] Типы дорог влияют на расход топлива
- [ ] Карта загружается в игре корректно

---

**Статус:** ✅ Утверждено  
**Формат:** JSON  
**Готово к созданию карты**
