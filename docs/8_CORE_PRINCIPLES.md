# 8 ГЛАВНЫХ ПРИНЦИПОВ ПРОЕКТА / 8 CORE PROJECT PRINCIPLES

**Road Trip Adventure RPG**

**Версия:** 1.0  
**Дата:** 2025-11-12  
**Статус:** ОБЯЗАТЕЛЬНЫЕ К СОБЛЮДЕНИЮ

---

## ⚠️ ВАЖНО / CRITICAL

**Эти 8 принципов являются ОСНОВОЙ проекта.**

Каждое новое дополнение кода и проекта **НЕ ДОЛЖНО НАРУШАТЬ** этих принципов.

Нарушение любого принципа → **ОСТАНОВКА РАЗРАБОТКИ** до согласования.

---

## Принцип 1: Немедленная Работоспособность

### Формулировка / Statement
**Каждая фича должна работать здесь и сейчас**

### Описание / Description

**Русский:**
- Нет незавершенных функций
- Нет заглушек "временно"
- Нет комментариев "TODO: реализовать позже"
- Все работает сразу после реализации
- Компилируется без предупреждений
- Запускается без ошибок

**English:**
- No incomplete functions
- No temporary stubs
- No "TODO: implement later" comments
- Everything works immediately after implementation
- Compiles without warnings
- Runs without errors

### Примеры / Examples

#### ✅ ПРАВИЛЬНО / CORRECT
```cpp
// Полностью реализованная функция
void Player::refuel(float amount) {
    if (amount <= 0) return;
    if (!canAfford(amount * FUEL_PRICE)) {
        showMessage("Недостаточно денег");
        return;
    }
    
    m_fuel = std::min(m_fuel + amount, MAX_FUEL);
    m_money -= amount * FUEL_PRICE;
    updateHUD();
}
```

#### ❌ НЕПРАВИЛЬНО / WRONG
```cpp
// Заглушка "временно"
void Player::refuel(float amount) {
    // TODO: реализовать позже
    m_fuel += amount;
}
```

### Проверка / Checklist
- [ ] Функция полностью реализована?
- [ ] Все edge cases обработаны?
- [ ] Нет TODO/FIXME/HACK комментариев?
- [ ] Код компилируется?
- [ ] Код работает как ожидается?

---

## Принцип 2: Независимость и Согласование

### Формулировка / Statement
**Фича не должна иметь зависимостей не прописанных в ТЗ. Если требуется дополнительная реализация → СТОП**

### Описание / Description

**Русский:**
- Работаем ТОЛЬКО из утвержденного ТЗ
- Обнаружена непредусмотренная зависимость → **ОСТАНОВКА**
- Запрос на согласование владельцу проекта
- Разработка продолжается ТОЛЬКО после одобрения
- Документируем все новые зависимости

**English:**
- Work ONLY from approved specifications
- Discovered undocumented dependency → **STOP**
- Request approval from project owner
- Development continues ONLY after approval
- Document all new dependencies

### Примеры / Examples

#### ✅ ПРАВИЛЬНО / CORRECT

**Ситуация:** Реализуем ShopScene, в ТЗ указано использовать GridInventorySystem.

```markdown
# Отчет в progress.md
## ShopScene Implementation
- ✅ Используется GridInventorySystem (указан в ТЗ)
- ✅ Используется ResourceManager (указан в ТЗ)
- ✅ UI components из существующей системы
- Все зависимости были в ТЗ, разработка завершена
```

#### ❌ НЕПРАВИЛЬНО / WRONG

**Ситуация:** Реализуем ShopScene, обнаружили что нужна система скидок (не в ТЗ).

```markdown
# НЕПРАВИЛЬНО - продолжаем разработку
## ShopScene Implementation
- ✅ Добавлена система скидок (DiscountSystem)
- ✅ Добавлена система лояльности (LoyaltySystem)
- Реализовал по своему усмотрению
```

**ПРАВИЛЬНО:**
```markdown
# ПРАВИЛЬНО - остановка
## ShopScene Implementation - STOPPED
⚠️ **ТРЕБУЕТСЯ СОГЛАСОВАНИЕ**

Обнаружена необходимость:
- DiscountSystem - для скидок в магазине
- LoyaltySystem - для постоянных клиентов

Эти системы НЕ указаны в ТЗ.

**Действия:**
1. СТОП разработки ShopScene
2. Запрос согласования у владельца
3. Ожидание решения:
   - Одобрить добавление систем
   - ИЛИ упростить реализацию без них

**Статус:** WAITING FOR APPROVAL
```

### Проверка / Checklist
- [ ] Все зависимости есть в ТЗ?
- [ ] Не требуется ничего дополнительного?
- [ ] Если требуется → остановлен и создан запрос?
- [ ] Получено одобрение на все новые зависимости?

---

## Принцип 3: Строгое Соответствие ТЗ

### Формулировка / Statement
**Каждая фича добавляется исключительно из ТЗ и работает строго как прописано**

### Описание / Description

**Русский:**
- Никаких "улучшений" без согласования
- Точное следование спецификации
- Реализация ТОЛЬКО того, что явно указано
- Не добавлять "было бы круто если..."
- Не убирать требования "это же очевидно устарело"

**English:**
- No "improvements" without approval
- Exact adherence to specification
- Implement ONLY what is explicitly stated
- Don't add "it would be cool if..."
- Don't remove requirements "this is obviously outdated"

### Примеры / Examples

#### ✅ ПРАВИЛЬНО / CORRECT

**ТЗ:**
```
ShopScene должна:
1. Показать список из 5 товаров
2. Позволить купить товар за деньги
3. Добавить товар в инвентарь
4. ESC - закрыть магазин
```

**Реализация:**
```cpp
class ShopScene : public Scene {
    std::vector<Item> m_items;  // 5 товаров
    
    void buyItem(int index) {
        // Точно как в ТЗ
        if (!canAfford(m_items[index].price)) return;
        
        m_money -= m_items[index].price;
        m_inventory.addItem(m_items[index]);
    }
    
    void handleInput(const sf::Event& event) {
        if (event.key.code == sf::Keyboard::Escape) {
            finish();  // Точно как в ТЗ
        }
    }
};
```

#### ❌ НЕПРАВИЛЬНО / WRONG

**Реализация с "улучшениями":**
```cpp
class ShopScene : public Scene {
    std::vector<Item> m_items;  // 10 товаров (БЫЛО 5!)
    
    void buyItem(int index) {
        // Добавил систему торга (НЕТ В ТЗ!)
        float finalPrice = negotiatePrice(m_items[index].price);
        
        m_money -= finalPrice;
        m_inventory.addItem(m_items[index]);
        
        // Добавил систему достижений (НЕТ В ТЗ!)
        unlockAchievement("FIRST_PURCHASE");
    }
    
    void handleInput(const sf::Event& event) {
        // Добавил Q для быстрого выхода (НЕТ В ТЗ!)
        if (event.key.code == sf::Keyboard::Q || 
            event.key.code == sf::Keyboard::Escape) {
            finish();
        }
    }
};
```

### Проверка / Checklist
- [ ] Все из ТЗ реализовано?
- [ ] Ничего сверх ТЗ не добавлено?
- [ ] Поведение точно как описано?
- [ ] Нет собственных "улучшений"?

---

## Принцип 4: Единый План Разработки

### Формулировка / Statement
**Все идет строго по плану, никакой разветвленной разработки и отклонения от архитектуры**

### Описание / Description

**Русский:**
- Следуем MASTER_IMPLEMENTATION_PLAN.md
- Никаких отклонений от архитектуры
- Последовательное выполнение задач
- Запрет на параллельные эксперименты
- Один план, одно направление

**English:**
- Follow MASTER_IMPLEMENTATION_PLAN.md
- No deviations from architecture
- Sequential task execution
- No parallel experiments
- One plan, one direction

### Примеры / Examples

#### ✅ ПРАВИЛЬНО / CORRECT

**План в tasks.md:**
```
Phase 1: WorldMapScene
Phase 2: CinematicEventScene
Phase 3: LocationScene
```

**Разработка:**
```markdown
Week 1-2: WorldMapScene
- ✅ Реализована базовая карта
- ✅ Реализована навигация
- ✅ Тесты пройдены
- ✅ Phase 1 ЗАВЕРШЕНА

Week 3: Начало Phase 2 (CinematicEventScene)
```

#### ❌ НЕПРАВИЛЬНО / WRONG

**Разработка:**
```markdown
Week 1: WorldMapScene (50% готово)
+ Параллельно начал LocationScene (не по плану!)
+ Экспериментирую с новой архитектурой UI (отклонение!)
+ Переписал SceneManager на другой паттерн (нарушение!)

Результат: 3 незавершенные задачи, сломанная архитектура
```

### Проверка / Checklist
- [ ] Следуем плану из MASTER_IMPLEMENTATION_PLAN.md?
- [ ] Текущая фаза завершена перед переходом к следующей?
- [ ] Нет параллельных экспериментов?
- [ ] Архитектура не нарушена?

---

## Принцип 5: Отдельная Директория для Отчетов

### Формулировка / Statement
**Все отчетные файлы должны быть в отдельной директории**

### Описание / Description

**Русский:**
- Все отчеты в `docs/progress/`
- Архив в `docs/progress/archived/`
- НЕ захламлять основные директории
- Чистота структуры проекта
- Легко найти текущий статус

**English:**
- All reports in `docs/progress/`
- Archive in `docs/progress/archived/`
- DON'T clutter main directories
- Clean project structure
- Easy to find current status

### Структура / Structure

```
✅ ПРАВИЛЬНО / CORRECT:
docs/
├── progress.md              # Основной файл прогресса
├── progress/                # Директория отчетов
│   ├── 2025-11-12_feature_x.md
│   ├── 2025-11-13_bugfix_y.md
│   └── archived/           # Старые отчеты
│       └── 2025-11-10_*.md

❌ НЕПРАВИЛЬНО / WRONG:
docs/
├── progress.md
├── MY_REPORT.md            # НЕ В ДИРЕКТОРИИ!
├── feature_status.md        # НЕ В ДИРЕКТОРИИ!
└── tasks/
    └── my_progress.md      # НЕПРАВИЛЬНОЕ МЕСТО!
```

### Проверка / Checklist
- [ ] Все отчеты в `docs/progress/`?
- [ ] Старые отчеты в `archived/`?
- [ ] Основные директории чистые?
- [ ] Легко найти текущий статус?

---

## Принцип 6: UI + Assets При Реализации

### Формулировка / Statement
**Если фича имеет какое-то отражение в UI, она должна быть сразу добавлена в UI и должен появиться запрос на создание ассета для нее**

### Описание / Description

**Русский:**
- Фича в UI? → Реализуем UI сразу
- Нужен ассет? → Создаем запрос сразу
- НЕ откладываем "на потом"
- НЕ "сделаем UI позже"
- Полная интеграция сразу

**English:**
- Feature in UI? → Implement UI immediately
- Need asset? → Create request immediately
- DON'T postpone "for later"
- NO "we'll add UI later"
- Full integration right away

### Примеры / Examples

#### ✅ ПРАВИЛЬНО / CORRECT

**Задача:** Добавить Shop button в TownScene

```markdown
## ShopButton Implementation

1. ✅ Добавлена кнопка в UI
2. ✅ Создан запрос на ассет:
   - `assets/ui/shop_button.png` (64x64)
   - `assets/ui/shop_button_hover.png` (64x64)
3. ✅ Временно используется placeholder
4. ✅ Функционал полностью работает
5. ✅ Готово к замене ассета

Status: COMPLETE (ожидание ассета)
```

#### ❌ НЕПРАВИЛЬНО / WRONG

**Задача:** Добавить Shop button в TownScene

```markdown
## ShopButton Implementation

1. ✅ Добавлена логика кнопки в коде
2. ⏳ UI добавим позже
3. ⏳ Ассеты запросим когда UI сделаем

Status: Логика готова (UI потом)
```

### Шаблон Запроса Ассета / Asset Request Template

```markdown
# Asset Request

**Feature:** [название фичи]
**Type:** [UI/Sprite/Sound/Music]
**Priority:** [High/Medium/Low]

## Needed Assets:

1. **shop_button.png**
   - Size: 64x64 pixels
   - Style: Pixel-art, retro
   - States: Normal, Hover, Pressed
   - Format: PNG with transparency

2. **shop_icon.png**
   - Size: 32x32 pixels
   - Style: Matching button
   - Format: PNG with transparency

## Temporary Solution:
Using colored rectangles as placeholders

## Integration Status:
- ✅ UI component created
- ✅ Placeholder working
- ⏳ Waiting for assets
```

### Проверка / Checklist
- [ ] Фича в UI? → UI реализован сразу?
- [ ] Нужны ассеты? → Запрос создан?
- [ ] Используются placeholders временно?
- [ ] Готово к замене на финальные ассеты?

---

## Принцип 7: Разветвленная Структура

### Формулировка / Statement
**Все реализуется строго по разветвленной структуре**

### Описание / Description

**Русский:**
- Модульная организация кода
- Каждый компонент в своем блоке/файле
- Четкое разделение ответственности
- Нет "God Objects"
- Нет смешивания логики

**English:**
- Modular code organization
- Each component in its own block/file
- Clear separation of responsibilities
- No "God Objects"
- No logic mixing

### Структура / Structure

```
✅ ПРАВИЛЬНО / CORRECT:
src/
├── scenes/              # Сцены
│   ├── WorldMapScene.cpp
│   ├── TownScene.cpp
│   └── ShopScene.cpp
├── systems/             # Системы
│   ├── ResourceManager.cpp
│   ├── InventorySystem.cpp
│   └── EventManager.cpp
├── ui/                  # UI компоненты
│   ├── UIButton.cpp
│   ├── UIPanel.cpp
│   └── HUDPanel.cpp
└── data/               # Данные
    ├── Item.cpp
    └── NPC.cpp

❌ НЕПРАВИЛЬНО / WRONG:
src/
├── game.cpp            # ВСЁ В ОДНОМ ФАЙЛЕ!
└── utils.cpp           # Куча несвязанного
```

### Примеры / Examples

#### ✅ ПРАВИЛЬНО / CORRECT

```cpp
// ShopScene.cpp - ТОЛЬКО логика сцены магазина
class ShopScene : public Scene {
    void handleInput() { /* только input */ }
    void update() { /* только обновление */ }
    void render() { /* только отрисовка */ }
};

// ShopInventory.cpp - ТОЛЬКО логика инвентаря магазина
class ShopInventory {
    void addItem() { /* только управление товарами */ }
    void removeItem() { /* только управление товарами */ }
};

// ShopUI.cpp - ТОЛЬКО UI магазина
class ShopUI {
    void renderItemList() { /* только UI */ }
    void renderPrices() { /* только UI */ }
};
```

#### ❌ НЕПРАВИЛЬНО / WRONG

```cpp
// ShopScene.cpp - ВСЁ ВМЕСТЕ!
class ShopScene : public Scene {
    // Логика сцены
    // + логика инвентаря
    // + логика UI
    // + логика ценообразования
    // + логика NPC
    // = 2000 строк кода в одном файле
};
```

### Проверка / Checklist
- [ ] Каждый класс в своем файле?
- [ ] Четкое разделение ответственности?
- [ ] Нет файлов >500 строк?
- [ ] Нет "God Objects"?

---

## Принцип 8: Отражение в Progress

### Формулировка / Statement
**Каждое изменение кода отражается в соответствии в прогресс файле**

### Описание / Description

**Русский:**
- Обязательное обновление `docs/progress.md`
- После каждого значимого изменения
- Документирование всех изменений
- Прозрачность разработки
- История решений

**English:**
- Mandatory update of `docs/progress.md`
- After each significant change
- Document all changes
- Development transparency
- Decision history

### Формат Записи / Entry Format

```markdown
## [Дата] - [Название Изменения]

### Что Сделано / What's Done
- ✅ [Изменение 1]
- ✅ [Изменение 2]
- ✅ [Изменение 3]

### Затронутые Файлы / Affected Files
- `src/ShopScene.cpp` - добавлена логика покупки
- `include/ShopScene.h` - добавлены методы
- `docs/task-tech.md` - обновлена спецификация

### Следование Принципам / Principles Compliance
- [x] Принцип 1: Работает сразу
- [x] Принцип 2: Все из ТЗ
- [x] Принцип 3: Строго по ТЗ
- [x] Принцип 6: UI + Asset запрос
- [x] Принцип 7: Модульная структура

### Следующие Шаги / Next Steps
- [ ] Ожидание ассетов для UI
- [ ] Тестирование после интеграции
```

### Проверка / Checklist
- [ ] `progress.md` обновлен?
- [ ] Все изменения задокументированы?
- [ ] Указаны затронутые файлы?
- [ ] Отмечено следование принципам?

---

## 📋 Чеклист Соблюдения Всех Принципов / All Principles Checklist

Перед завершением ЛЮБОЙ задачи проверь:

### Принцип 1: Немедленная Работоспособность
- [ ] Фича работает прямо сейчас?
- [ ] Нет TODO/заглушек?

### Принцип 2: Независимость и Согласование
- [ ] Все зависимости из ТЗ?
- [ ] Если нужно дополнительное → согласовано?

### Принцип 3: Строгое Соответствие ТЗ
- [ ] Реализовано точно как в ТЗ?
- [ ] Нет "улучшений"?

### Принцип 4: Единый План
- [ ] Следуем плану из MASTER_IMPLEMENTATION_PLAN.md?
- [ ] Нет отклонений от архитектуры?

### Принцип 5: Отдельная Директория
- [ ] Отчеты в `docs/progress/`?

### Принцип 6: UI + Assets
- [ ] Если UI → реализован сразу?
- [ ] Если нужны ассеты → запрос создан?

### Принцип 7: Разветвленная Структура
- [ ] Модульная организация?
- [ ] Четкое разделение ответственности?

### Принцип 8: Отражение в Progress
- [ ] `progress.md` обновлен?
- [ ] Все изменения задокументированы?

---

## ⚠️ Что Делать При Нарушении / What To Do If Violated

### Если Обнаружено Нарушение:

1. **НЕМЕДЛЕННАЯ ОСТАНОВКА** разработки
2. **ОТКАТ** к последнему корректному состоянию
3. **АНАЛИЗ** причины нарушения
4. **СОГЛАСОВАНИЕ** с владельцем проекта
5. **ИСПРАВЛЕНИЕ** с соблюдением всех принципов
6. **ДОКУМЕНТИРОВАНИЕ** инцидента

### Пример Отчета о Нарушении:

```markdown
# ⚠️ VIOLATION REPORT

**Date:** 2025-11-12
**Principle Violated:** Принцип 3 (Строгое соответствие ТЗ)

## What Happened:
Добавлена система торга в ShopScene, которая не была в ТЗ.

## Impact:
- Дополнительные 200 строк кода
- Зависимость от NPCManager (не в ТЗ)
- Усложнение логики

## Actions Taken:
1. ✅ Остановлена разработка
2. ✅ Откат к предыдущему коммиту
3. ✅ Создан запрос на согласование
4. ⏳ Ожидание решения владельца

## Status:
WAITING FOR APPROVAL
```

---

## 📚 Дополнительные Ресурсы / Additional Resources

- [PROMPT_TEMPLATE.md](PROMPT_TEMPLATE.md) - Шаблоны с применением принципов
- [tasks.md](tasks.md) - ТЗ и требования
- [task-tech.md](task-tech.md) - Технические спецификации
- [tech.md](tech.md) - Архитектура проекта
- [progress.md](progress.md) - История разработки

---

**Статус:** ОБЯЗАТЕЛЬНЫЕ К СОБЛЮДЕНИЮ  
**Версия:** 1.0  
**Дата:** 2025-11-12  
**Автор:** Project Owner

**ЭТИ ПРИНЦИПЫ НЕ ОБСУЖДАЮТСЯ. ТОЛЬКО СОБЛЮДАЮТСЯ.**
