# АКТУАЛЬНЫЙ ПЛАН РАЗРАБОТКИ
# "Дневник путешественника" - Блокнот с ветвлением сюжета

**Версия:** 2.0 (Переработанная)
**Дата:** 2025-12-07
**Статус:** 🎯 АКТИВНАЯ РАЗРАБОТКА

---

## 🎯 ГЛАВНАЯ КОНЦЕПЦИЯ

**"Дорога разделяется не на две, а на тысячу возможностей. Но выбрать можно лишь одну."**

### Ключевые принципы:

1. **Весь геймплей = Блокнот + Выбор**
   - Никаких визуальных сцен с движением
   - Всё повествование через записи в дневнике
   - Выборы меняют не просто числа, а открывают новый контент

2. **Радикальное ветвление**
   - Каждый важный выбор → принципиально разный контент
   - Трасса vs Проселок = две разные игры в одной
   - Уникальные события для каждой ветки

3. **Живой мир во время выбора**
   - Система размышлений (всплывающие мысли)
   - Фоновые звуки и музыка
   - Плавное изменение ресурсов в реальном времени
   - Игрок не чувствует "паузы" даже при долгих раздумьях

4. **Полноценные сохранения**
   - История всех выборов
   - Автосохранение каждые 10 минут
   - Возможность откатиться и попробовать другой путь

---

## 📋 АКТУАЛЬНЫЙ ПЛАН РАЗРАБОТКИ (30 ДНЕЙ)

### НЕДЕЛЯ 1: CORE СИСТЕМЫ (Дни 1-7)

#### **День 1-2: NotebookScene - Основа блокнота**

**Цель:** Заменить JournalScene на NotebookScene с анимацией печати текста

**Задачи:**
1. Создать `include/Notebook/NotebookScene.h`
2. Создать `src/Notebook/NotebookScene.cpp`
3. Реализовать анимацию печати текста (символ за символом)
4. Добавить пропуск анимации (пробел/Enter)
5. Интегрировать в SceneManager

**Критерии готовности:**
- ✅ Текст печатается плавно (50 символов/секунду)
- ✅ Можно пропустить анимацию
- ✅ Работает рендеринг на фоне старой бумаги
- ✅ HUD отображается корректно

**Файлы:**
```
include/Notebook/NotebookScene.h
src/Notebook/NotebookScene.cpp
```

---

#### **День 3-4: NotebookEntry - Структура записей**

**Цель:** Создать систему управления записями блокнота

**Задачи:**
1. Создать `include/Notebook/NotebookEntry.h`
2. Создать `src/Notebook/NotebookEntry.cpp`
3. Реализовать типы записей (PRESENT, PAST, THOUGHT)
4. Добавить систему выборов с таймерами
5. Поддержка пропуска текста

**Структура NotebookEntry:**
```cpp
struct NotebookChoice {
    std::string text;
    std::function<void(PlayerState*)> action;
    std::vector<std::string> nextEntryIds;  // Ветвление!
    float probability = 1.0f;               // Вероятность перехода

    // Требования
    float energyRequired = 0.0f;
    float fuelRequired = 0.0f;
    bool isDisabled = false;
    std::string disabledReason;
};

enum class EntryType {
    PRESENT,   // Настоящее время: выбор здесь и сейчас
    PAST,      // Воспоминание: просто чтение, без выбора
    THOUGHT    // Внутренний монолог: размышления
};

struct NotebookEntry {
    std::string id;
    EntryType type;
    std::string text;
    std::vector<NotebookChoice> choices;

    // Анимация
    float printSpeed = 50.0f;  // символов/секунду
    bool canSkip = true;

    // Ветвление
    std::string branchId;  // "highway" или "backroad"
};
```

**Критерии готовности:**
- ✅ Записи с выборами работают
- ✅ Записи-воспоминания (без выборов) работают
- ✅ Можно переходить между записями

---

#### **День 5-6: GameState Serialization - Сохранения**

**Цель:** Реализовать систему сохранения/загрузки игры

**Задачи:**
1. Расширить `PlayerState` для сериализации
2. Создать `include/SaveManager.h`
3. Создать `src/SaveManager.cpp`
4. Реализовать сохранение в JSON формат
5. Добавить автосохранение каждые 10 минут

**Структура сохранения:**
```json
{
  "metadata": {
    "version": "1.0",
    "saveTime": "2025-12-07T15:30:00Z",
    "playTime": 3542.5,
    "slotName": "autosave"
  },

  "gameState": {
    "resources": {
      "energy": 65.5,
      "fuel": 28.0,
      "money": 1200.0
    },
    "progress": {
      "currentDay": 1,
      "dayTime": 14.5,
      "currentEntryId": "day1_fork"
    },
    "flags": {
      "chose_highway": true,
      "met_traveler": false
    }
  },

  "notebook": {
    "readEntries": ["tutorial", "day1_morning", "day1_fork"],
    "currentBranch": "highway",
    "choiceHistory": [
      {
        "entryId": "day1_fork",
        "choiceIndex": 0,
        "chosenText": "По трассе"
      }
    ]
  }
}
```

**Критерии готовности:**
- ✅ Сохранение работает в `saves/` директорию
- ✅ Загрузка восстанавливает состояние точно
- ✅ Автосохранение каждые 10 минут
- ✅ Список слотов сохранения доступен

---

#### **День 7: Тестирование + Интеграция**

**Цель:** Убедиться, что базовая система работает

**Задачи:**
1. Написать тесты для NotebookScene
2. Написать тесты для SaveManager
3. Протестировать полный цикл: запись → выбор → сохранение → загрузка
4. Исправить найденные баги

**Критерии готовности:**
- ✅ Все тесты проходят (100%)
- ✅ Игра работает от главного меню до записи и обратно
- ✅ Сохранения не ломаются

---

### НЕДЕЛЯ 2: СИСТЕМА РАЗМЫШЛЕНИЙ (Дни 8-14)

#### **День 8-9: ThoughtBubble System - Всплывающие мысли**

**Цель:** Добавить живость во время долгих выборов

**Задачи:**
1. Создать `include/Notebook/ThoughtSystem.h`
2. Создать `src/Notebook/ThoughtSystem.cpp`
3. Реализовать всплывающие мысли (правый нижний угол)
4. Добавить триггеры (после 30/60/90 секунд)
5. Загрузка мыслей из JSON

**Структура системы:**
```cpp
class ThoughtSystem {
public:
    struct ThoughtBubble {
        std::string text;
        float displayTime = 0.0f;
        float maxDisplayTime = 8.0f;
        sf::Vector2f position;
        bool isFading = false;
    };

    void loadThoughts(const std::string& jsonPath);
    void update(float deltaTime, float timeSinceChoice);
    void spawnRandomThought(const std::string& pool = "general");
    void spawnContextThought(const std::string& context);
    void render(sf::RenderWindow& window, const sf::Font& font);

private:
    std::map<std::string, std::vector<std::string>> m_thoughtPools;
    std::vector<ThoughtBubble> m_activeThoughts;
    float m_thoughtTimer = 0.0f;
};
```

**JSON конфигурация:**
```json
{
  "thoughtPools": {
    "general": [
      "В кармане осталось 500 рублей...",
      "Как там мама?",
      "Доеду ли до границы?",
      "А что в Германии?"
    ],
    "road": [
      "Бензин на исходе...",
      "Двигатель стучит странно",
      "Эта дорога бесконечна"
    ],
    "time_pressure": [
      "Темнеет...",
      "Уже поздно",
      "Нужно решать!"
    ]
  },

  "triggers": {
    "after_30_sec": {"chance": 0.3, "interval": 10.0},
    "after_60_sec": {"chance": 0.6, "interval": 7.0},
    "after_90_sec": {"chance": 0.9, "interval": 5.0}
  }
}
```

**Критерии готовности:**
- ✅ Мысли появляются автоматически при долгих выборах
- ✅ Мысли плавно появляются и исчезают
- ✅ Не перекрывают основной текст
- ✅ Разные пулы мыслей для разных ситуаций

---

#### **День 10-11: Dynamic Resources - Живые ресурсы**

**Цель:** Ресурсы меняются в реальном времени

**Задачи:**
1. Добавить в NotebookScene систему изменения ресурсов
2. Топливо медленно убывает во время выбора о поездке
3. Энергия медленно уменьшается ночью
4. Визуализация изменений в HUD

**Механика:**
```cpp
void NotebookScene::update(float deltaTime) {
    // Если выбор связан с поездкой
    if (m_currentEntry->branchId == "highway" && !m_choiceMade) {
        // Топливо убывает медленно (симуляция работы двигателя)
        m_playerState->modifyFuel(-0.1f * deltaTime);
    }

    // Если ночь
    if (m_playerState->getDayTime() > 22.0f && !m_choiceMade) {
        // Энергия убывает
        m_playerState->modifyEnergy(-0.2f * deltaTime);
    }

    // Обновить HUD с анимацией
    updateHUD(deltaTime);
}
```

**Критерии готовности:**
- ✅ Ресурсы медленно меняются во время выбора
- ✅ HUD плавно обновляется (анимация)
- ✅ Игрок замечает изменения

---

#### **День 12-13: Ambient Sounds - Фоновые звуки**

**Цель:** Добавить атмосферу звуками

**Задачи:**
1. Добавить поддержку SFML Audio
2. Загрузка фоновых звуков (engine.ogg, wind.ogg, city.ogg)
3. Проигрывание звуков в зависимости от контекста
4. Плавное затухание/нарастание громкости

**Звуки для MVP:**
- `engine_idle.ogg` - работа двигателя на холостых
- `wind.ogg` - ветер в поле
- `city_ambience.ogg` - шум города
- `night_crickets.ogg` - ночь в лесу

**Критерии готовности:**
- ✅ Звуки проигрываются в зависимости от записи
- ✅ Плавные переходы между звуками
- ✅ Можно отключить звук в настройках

---

#### **День 14: Тестирование системы размышлений**

**Цель:** Убедиться, что долгие выборы не скучны

**Задачи:**
1. Провести тест: выбор с 90-секундным раздумьем
2. Проверить, что мысли появляются своевременно
3. Проверить, что ресурсы меняются плавно
4. Проверить, что звуки не раздражают

**Критерии готовности:**
- ✅ Игрок не чувствует "простоя" при долгом выборе
- ✅ Мысли помогают атмосфере, не мешают
- ✅ Звуки создают погружение

---

### НЕДЕЛЯ 3: СИСТЕМА ВЕТВЛЕНИЯ (Дни 15-21)

#### **День 15-16: BranchingSystem - Управление ветками**

**Цель:** Создать систему управления ветвлением сюжета

**Задачи:**
1. Создать `include/Notebook/BranchingSystem.h`
2. Создать `src/Notebook/BranchingSystem.cpp`
3. Реализовать загрузку веток из JSON
4. Реализовать переключение между ветками
5. Реализовать блокировку событий в зависимости от ветки

**Структура:**
```cpp
class BranchingSystem {
public:
    struct StoryBranch {
        std::string branchId;              // "highway" или "backroad"
        std::vector<std::string> uniqueEvents;  // Уникальные события
        std::vector<std::string> blockedEvents; // Заблокированные события
        float progressMultiplier = 1.0f;        // Скорость прогресса
    };

    void loadBranches(const std::string& jsonPath);
    void setBranch(const std::string& branchId);
    bool isEventAvailable(const std::string& eventId) const;
    std::vector<NotebookEntry> getBranchEntries() const;

private:
    std::map<std::string, StoryBranch> m_branches;
    StoryBranch* m_currentBranch = nullptr;
};
```

**JSON конфигурация:**
```json
{
  "branches": {
    "highway": {
      "id": "highway",
      "uniqueEvents": [
        "highway_police_check",
        "highway_gas_station",
        "highway_hitchhiker"
      ],
      "blockedEvents": [
        "backroad_breakdown",
        "backroad_village"
      ],
      "progressMultiplier": 1.5
    },

    "backroad": {
      "id": "backroad",
      "uniqueEvents": [
        "backroad_breakdown",
        "backroad_abandoned_factory",
        "backroad_village"
      ],
      "blockedEvents": [
        "highway_police_check",
        "highway_gas_station"
      ],
      "progressMultiplier": 0.8
    }
  }
}
```

**Критерии готовности:**
- ✅ Ветки загружаются из JSON
- ✅ Переключение между ветками работает
- ✅ Уникальные события доступны только в своей ветке
- ✅ Блокированные события не появляются

---

#### **День 17-18: Day1 Content - Контент Дня 1**

**Цель:** Создать полный контент первого дня с ветвлением

**Задачи:**
1. Написать 15 записей для Дня 1
2. Создать критический выбор: трасса vs проселок
3. Создать 5 уникальных событий для трассы
4. Создать 5 уникальных событий для проселка
5. Балансировка последствий

**Структура Дня 1:**
```
08:00 - Утро (tutorial_start)
09:00 - Выезд (day1_departure)
10:00 - Гаишник (day1_cop)
11:00 - РАЗВИЛКА (day1_fork) ⭐ КРИТИЧЕСКИЙ ВЫБОР

ВЕТКА А: ТРАССА
11:30 - Пост ГАИ (highway_police_check)
13:00 - Попутчик (highway_hitchhiker)
15:00 - Заправка (highway_gas_station)
17:00 - Дальнобойщик (highway_trucker)
19:00 - Вечер на трассе (highway_evening)

ВЕТКА Б: ПРОСЕЛОК
12:00 - Поломка (backroad_breakdown)
14:00 - Завод (backroad_factory)
16:00 - Деревня (backroad_village)
18:00 - Лес (backroad_forest)
20:00 - Ночь в лесу (backroad_night)
```

**Критерии готовности:**
- ✅ 15 записей написаны и протестированы
- ✅ Выбор трассы ведет к уникальному контенту
- ✅ Выбор проселка ведет к уникальному контенту
- ✅ Оба пути проходимы до конца
- ✅ Повторное прохождение даёт другой опыт

---

#### **День 19-20: Event Loading - Загрузка событий**

**Цель:** Реализовать загрузку всех событий из JSON

**Задачи:**
1. Создать `include/Notebook/EventLoader.h`
2. Создать `src/Notebook/EventLoader.cpp`
3. Реализовать парсинг событий из JSON
4. Реализовать случайный выбор следующего события
5. Интегрировать с BranchingSystem

**JSON формат события:**
```json
{
  "id": "day1_fork",
  "type": "PRESENT",
  "branch": null,
  "text": "Дорога раздваивается.\n\n← М4, федеральная трасса\n→ Проселок через леса",

  "choices": [
    {
      "text": "По трассе (быстрее, заметнее)",
      "consequences": {
        "setBranch": "highway",
        "energyChange": -5,
        "fuelChange": -10
      },
      "nextEntries": [
        {"id": "highway_police_check", "probability": 0.7},
        {"id": "highway_smooth", "probability": 0.3}
      ]
    },
    {
      "text": "Проселок (медленнее, скрытнее)",
      "consequences": {
        "setBranch": "backroad",
        "energyChange": -15,
        "fuelChange": -8
      },
      "nextEntries": [
        {"id": "backroad_breakdown", "probability": 0.4},
        {"id": "backroad_smooth", "probability": 0.6}
      ]
    }
  ]
}
```

**Критерии готовности:**
- ✅ События загружаются из `assets/events/day1.json`
- ✅ Вероятностный выбор следующего события работает
- ✅ Последствия применяются корректно
- ✅ Ветвление работает автоматически

---

#### **День 21: Тестирование ветвления**

**Цель:** Убедиться, что ветвление работает идеально

**Задачи:**
1. Пройти полностью ветку "трасса"
2. Пройти полностью ветку "проселок"
3. Проверить, что уникальные события не пересекаются
4. Проверить, что сохранения работают с ветками
5. Исправить найденные баги

**Критерии готовности:**
- ✅ Обе ветки проходимы без ошибок
- ✅ Сохранение/загрузка сохраняет ветку
- ✅ Уникальные события появляются только в своей ветке
- ✅ Игрок чувствует "другую игру" при повторном прохождении

---

### НЕДЕЛЯ 4: ПОЛИРОВКА + MVP (Дни 22-30)

#### **День 22-23: UI Polish - Полировка интерфейса**

**Цель:** Сделать интерфейс приятным и интуитивным

**Задачи:**
1. Улучшить анимацию печати текста (плавность)
2. Добавить анимацию появления выборов
3. Улучшить визуализацию мыслей (fade in/out)
4. Добавить анимацию HUD (плавное изменение ресурсов)
5. Добавить подсказки управления

**Критерии готовности:**
- ✅ Интерфейс плавный и приятный
- ✅ Анимации не тормозят
- ✅ Управление интуитивно понятно

---

#### **День 24-25: Balance Pass - Балансировка**

**Цель:** Сбалансировать расход ресурсов и сложность

**Задачи:**
1. Балансировка расхода топлива (сколько тратится)
2. Балансировка расхода энергии (когда устаешь)
3. Балансировка стоимости услуг (заправка, мотель)
4. Настройка вероятностей событий
5. Настройка скорости прогресса

**Таблица баланса:**
```
ТРАССА:
- Прогресс: 250 км/день
- Расход топлива: 15L на 100км
- События: 70% опасные, 30% нейтральные
- Стоимость заправки: 600₽

ПРОСЕЛОК:
- Прогресс: 150 км/день
- Расход топлива: 12L на 100км (медленнее)
- События: 40% опасные, 60% нейтральные
- Стоимость заправки: 400₽ (деревня)
```

**Критерии готовности:**
- ✅ Оба пути сбалансированы по сложности
- ✅ Нет "очевидно лучшего" выбора
- ✅ Ресурсы хватает, но нужно планировать

---

#### **День 26-27: Sound & Music - Звук и музыка**

**Цель:** Добавить атмосферу звуком

**Задачи:**
1. Добавить фоновую музыку (меланхоличная)
2. Добавить звуки печати текста (опционально)
3. Добавить звуки выбора (клик)
4. Балансировка громкости
5. Настройки звука в главном меню

**Музыкальные треки:**
- `main_theme.ogg` - главная тема (меню)
- `highway.ogg` - дорожная музыка (ритмичная)
- `backroad.ogg` - проселочная музыка (тихая)
- `night.ogg` - ночная музыка (ambient)

**Критерии готовности:**
- ✅ Музыка подходит атмосфере
- ✅ Звуки не раздражают
- ✅ Можно настроить громкость

---

#### **День 28-29: Bug Fixing - Исправление багов**

**Цель:** Найти и исправить все баги

**Задачи:**
1. Полное прохождение обеих веток
2. Тестирование сохранений/загрузок
3. Тестирование системы размышлений
4. Исправление найденных багов
5. Оптимизация производительности

**Критерии готовности:**
- ✅ Нет критических багов
- ✅ Игра работает стабильно
- ✅ FPS > 60 постоянно

---

#### **День 30: MVP Release - Релиз MVP**

**Цель:** Подготовить демо-версию для тестирования

**Задачи:**
1. Создать README с инструкциями
2. Подготовить сборку для macOS
3. Создать Quick Start Guide
4. Записать демо-видео (опционально)
5. Релиз MVP для внутреннего тестирования

**Критерии готовности:**
- ✅ Игра запускается на чистой системе
- ✅ Инструкции понятны
- ✅ Демо-версия работает

---

## 🎯 КРИТЕРИИ УСПЕХА MVP

### Технические:
- ✅ Сборка без ошибок
- ✅ Все тесты проходят (100%)
- ✅ FPS > 60 стабильно
- ✅ Сохранения не ломаются
- ✅ Нет критических багов

### Геймплейные:
- ✅ Можно пройти День 1 полностью (обе ветки)
- ✅ Выбор трассы/проселка даёт разный контент (>70%)
- ✅ Повторное прохождение даёт другой опыт
- ✅ Система размышлений работает (мысли появляются)
- ✅ Долгие выборы не скучны (звуки + мысли + изменения)

### UX/UI:
- ✅ Интерфейс интуитивно понятен
- ✅ Анимации плавные
- ✅ Управление отзывчиво
- ✅ Шрифт читаем (кириллица)
- ✅ Цветовая палитра приятна

---

## 📊 МЕТРИКИ ДЛЯ ТЕСТИРОВАНИЯ

После релиза MVP собрать метрики:

### Вовлеченность:
- Среднее время прохождения Дня 1: ~30-40 минут
- Процент игроков, прошедших обе ветки: >50%
- Процент игроков, заметивших мысли: >80%
- Процент игроков, дочитавших до конца: >70%

### Ветвление:
- Выбор трассы: ~60%
- Выбор проселка: ~40%
- Обнаружение уникальных событий: >70%
- Чувство "другой игры" при повторном прохождении: 8/10

### Технические:
- Среднее FPS: >60
- Количество сбоев: 0
- Время загрузки сохранения: <1 секунда
- Размер сохранения: <100KB

---

## 🚀 СЛЕДУЮЩИЕ ЭТАПЫ (ПОСЛЕ MVP)

### Фаза 2: Контент (Дни 31-45)
- День 2: Граница, новые выборы
- День 3: Германия, финал
- Добавить 10+ уникальных событий на ветку
- Система NPC (спутники в дороге)

### Фаза 3: Системы (Дни 46-60)
- Система погоды (дождь, снег)
- Система времени суток (визуализация)
- Система репутации
- Расширенные концовки

### Фаза 4: Полировка (Дни 61-75)
- Профессиональные иллюстрации
- Профессиональная музыка
- Озвучка (опционально)
- Локализация на английский

---

## 📁 СТРУКТУРА ПРОЕКТА (ФИНАЛЬНАЯ)

```
kurs/
├── assets/
│   ├── events/
│   │   ├── day1.json          // События Дня 1
│   │   ├── highway_branch.json // Уникальные события трассы
│   │   └── backroad_branch.json // Уникальные события проселка
│   ├── thoughts/
│   │   └── thoughts.json       // Пулы мыслей
│   ├── sounds/
│   │   ├── engine_idle.ogg
│   │   ├── wind.ogg
│   │   ├── city_ambience.ogg
│   │   └── night_crickets.ogg
│   ├── music/
│   │   ├── main_theme.ogg
│   │   ├── highway.ogg
│   │   ├── backroad.ogg
│   │   └── night.ogg
│   └── fonts/
│       └── font.ttf
│
├── include/
│   ├── Notebook/
│   │   ├── NotebookScene.h     ⭐ Основная сцена блокнота
│   │   ├── NotebookEntry.h     ⭐ Структура записи
│   │   ├── ThoughtSystem.h     ⭐ Система мыслей
│   │   ├── BranchingSystem.h   ⭐ Система ветвления
│   │   └── EventLoader.h       ⭐ Загрузка событий из JSON
│   ├── SaveManager.h           ⭐ Система сохранений
│   └── ... (остальные заголовки)
│
├── src/
│   ├── Notebook/
│   │   ├── NotebookScene.cpp
│   │   ├── NotebookEntry.cpp
│   │   ├── ThoughtSystem.cpp
│   │   ├── BranchingSystem.cpp
│   │   └── EventLoader.cpp
│   ├── SaveManager.cpp
│   └── ... (остальные реализации)
│
├── saves/                      // Директория для сохранений
│   ├── autosave.json
│   ├── slot1.json
│   └── slot2.json
│
├── tests/
│   └── ... (модульные тесты)
│
├── TECHNICAL_DOCUMENTATION.md  // Техническая документация
├── ACTUAL_DEVELOPMENT_PLAN.md  // Этот файл
└── README.md                   // Инструкция для пользователей
```

---

## 🔑 КЛЮЧЕВЫЕ ПРАВИЛА

1. **Каждый важный выбор открывает новый контент, не просто меняет числа**
2. **Долгие выборы = живой мир (мысли + звуки + изменения)**
3. **Ветвление = радикально разный опыт**
4. **Сохранения = история всех выборов**
5. **MVP = полный День 1 с двумя ветками**

---

**Версия плана:** 2.0 (Переработанная)
**Статус:** 🎯 ГОТОВ К РАЗРАБОТКЕ
**Приоритет:** NotebookScene → Сохранения → Размышления → Ветвление → Контент

---

*"Дорога разделяется не на две, а на тысячу возможностей. Но выбрать можно лишь одну."*
