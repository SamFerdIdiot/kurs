# 🔧 Scripts / Скрипты

Эта папка содержит скрипты для сборки, тестирования и запуска проекта "69 Crossroads - Россия 90-х".

This folder contains scripts for building, testing, and running the "69 Crossroads" project.

---

## 📋 Доступные скрипты / Available Scripts

### 1. `build.sh` - Сборка проекта / Build Project
**Описание / Description:**
Универсальный скрипт сборки, который автоматически определяет операционную систему и находит SFML.

**Использование / Usage:**
```bash
./scripts/build.sh
```

**Что делает / What it does:**
- Автоматически определяет macOS или Linux
- На macOS ищет SFML в стандартных путях Homebrew:
  - `/opt/homebrew/opt/sfml@2/lib/cmake/SFML` (M1/M2 Mac)
  - `/opt/homebrew/Cellar/sfml/2.6.1/lib/cmake/SFML`
  - `/usr/local/opt/sfml@2/lib/cmake/SFML` (Intel Mac)
  - `/usr/local/lib/cmake/SFML`
- На Linux использует системный SFML
- Создаёт директорию `build/` если её нет
- Компилирует проект в Release режиме
- Показывает расположение исполняемого файла

**Требования / Requirements:**
- macOS: `brew install sfml@2`
- Linux: `sudo apt-get install libsfml-dev`

---

### 2. `test.sh` - Запуск тестов / Run Tests
**Описание / Description:**
Запускает все unit тесты проекта.

**Использование / Usage:**
```bash
./scripts/test.sh
```

**Что делает / What it does:**
- Проверяет наличие директории `build/`
- Собирает тесты если нужно
- Запускает unit тесты (`./tests/unit_tests`)
- Запускает CTest
- Показывает результаты

**Требования / Requirements:**
- Проект должен быть собран (`./scripts/build.sh`)

---

### 3. `run.sh` - Запуск игры / Run Game
**Описание / Description:**
Запускает игру.

**Использование / Usage:**
```bash
./scripts/run.sh
```

**Что делает / What it does:**
- Проверяет наличие исполняемого файла
- Собирает проект если нужно
- Запускает игру

---

### 4. `clean.sh` - Очистка / Clean Build
**Описание / Description:**
Удаляет директорию сборки.

**Использование / Usage:**
```bash
./scripts/clean.sh
```

**Что делает / What it does:**
- Удаляет `build/` директорию
- Полезно для полной пересборки проекта

---

### 5. `run_unit_tests.sh` - Только unit тесты / Unit Tests Only
**Описание / Description:**
Запускает только unit тесты (без CTest).

**Использование / Usage:**
```bash
./scripts/run_unit_tests.sh
```

---

### 6. `run_all_tests.sh` - Все тесты / All Tests
**Описание / Description:**
Собирает и запускает все тесты используя CTest.

**Использование / Usage:**
```bash
./scripts/run_all_tests.sh
```

---

## 🚀 Быстрый старт / Quick Start

### Первая сборка / First Build:
```bash
# 1. Клонировать репозиторий
git clone https://github.com/barzikol/kkurs.git
cd kkurs

# 2. Установить SFML (macOS)
brew install sfml@2

# 3. Собрать проект
./scripts/build.sh

# 4. Запустить тесты
./scripts/test.sh

# 5. Запустить игру
./scripts/run.sh
```

### Ежедневная разработка / Daily Development:
```bash
# После изменений в коде:
./scripts/build.sh && ./scripts/test.sh

# Или быстрая сборка и запуск:
./scripts/build.sh && ./scripts/run.sh
```

---

## 🔧 Решение проблем / Troubleshooting

### Проблема: "SFML not found"
**Решение:**
```bash
# macOS:
brew install sfml@2

# Linux:
sudo apt-get update
sudo apt-get install libsfml-dev
```

### Проблема: "Permission denied"
**Решение:**
```bash
# Сделать скрипты исполняемыми:
chmod +x scripts/*.sh
```

### Проблема: "Build directory not found"
**Решение:**
```bash
# Сначала собрать проект:
./scripts/build.sh
```

### Проблема: SFML в нестандартном месте
**Решение:**
```bash
# Вручную указать путь к SFML:
cd build
cmake .. -DSFML_DIR=/path/to/SFML
cmake --build .
```

---

## 📊 Статус сборки / Build Status

### Поддерживаемые платформы / Supported Platforms:
- ✅ macOS (Intel) - протестировано
- ✅ macOS (M1/M2) - протестировано
- ✅ Linux (Ubuntu 20.04+) - протестировано
- ❓ Windows - не поддерживается этими скриптами (используйте CMake напрямую)

### Требования / Requirements:
- **CMake**: 3.10 или выше
- **Компилятор**: GCC 13+ или Clang 14+
- **C++ Standard**: C++17
- **SFML**: 2.5 или выше (рекомендуется 2.6.1)

---

## 📝 Примеры / Examples

### Пример 1: Полная пересборка
```bash
./scripts/clean.sh
./scripts/build.sh
./scripts/test.sh
```

### Пример 2: Быстрая итерация
```bash
# После изменения кода:
./scripts/build.sh && ./scripts/test.sh
```

### Пример 3: Только запуск
```bash
./scripts/run.sh
```

---

## 🎯 Для CI/CD

Эти скрипты можно использовать в CI/CD пайплайнах:

```yaml
# Пример для GitHub Actions
steps:
  - name: Install SFML
    run: sudo apt-get install libsfml-dev
    
  - name: Build
    run: ./scripts/build.sh
    
  - name: Test
    run: ./scripts/test.sh
```

---

## 🔗 Связанные файлы / Related Files

- `CMakeLists.txt` - конфигурация CMake
- `UI_IMPLEMENTATION_PROMPT.md` - инструкции по реализации UI
- `PROJECT_STATUS.md` - статус проекта
- `DEVELOPMENT_ROADMAP.md` - план развития

---

**Обновлено / Updated**: 2025-11-17  
**Автор / Author**: @copilot  
**Для / For**: @denisov2956151998_barzikol
